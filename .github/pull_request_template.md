## What this changes, and why

<!-- The problem first, then the change. -->

## How it was checked

<!-- Tests added, and anything verified by hand or against a live page or provider. -->

- [ ] `pnpm lint && pnpm typecheck && pnpm test && pnpm ext` pass
- [ ] A provider or model change is recorded in `docs/providers.md`, with the source and the date
- [ ] Anything that changes what the software does is reflected in the landing page, the privacy page
      and `store/listing.md`
- [ ] A change to the browser agent's rules explains the reason, and the test that holds the new line
