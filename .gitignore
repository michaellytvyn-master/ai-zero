node_modules/
dist/
.next/
*.tsbuildinfo
.DS_Store

# Matches every env variant, including names with stray whitespace: a file
# called ".env " is not matched by a plain ".env" pattern and would otherwise
# be committed the moment it stops being empty.
.env*
!.env.example

# Regenerated by next dev and next build with different contents each time.
apps/web/next-env.d.ts
